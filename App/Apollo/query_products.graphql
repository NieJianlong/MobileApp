
  query GetListings($searchOptions: SearchOptions) {
    getListings(searchOptions: $searchOptions) {
      content{
          photo
         }
      pageNo
      pageSize
      totalPages
      totalElements
    }
  }